# @mixsight/types

OpenAPI-generated TypeScript types for the MixSight API. Per SCOPE.md §6.2:

> Type sharing: OpenAPI → TypeScript via `openapi-typescript`. Regenerate on every backend schema change.

Never hand-edit the generated output — it's overwritten on every codegen run.
