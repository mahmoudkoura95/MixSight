# @mixsight/shared

Cross-app constants, enums, and validation helpers shared between `apps/web` and codegen consumers of `apps/api`. Per SCOPE.md §6.1.

**Keep this lean.** When in doubt, inline in the consuming app. Shared code is a maintenance debt that grows quietly.
